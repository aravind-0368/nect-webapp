import os

markdown\_content \= """\# Product Requirements Document (PRD)  
\#\# Project Name: Nect (Gamified Life-OS Ecosystem)  
\*\*Author:\*\* Product Development Team    
\*\*Tech Stack:\*\* React, Next.js, Prisma, PostgreSQL (Supabase/Neon), Vercel, Tailwind CSS    
\*\*Target Audience:\*\* Small Scale (10–15 Users, Closed Beta)  

\---

\#\# 1\. Executive Summary & Core Concept

\*\*Nect\*\* is a unified "Life-OS" web application designed to consolidate productivity, health, finance, and education tracking into a single, cohesive dashboard. Unlike isolated tracking utilities, Nect introduces a central \*\*Gamification Engine\*\* that binds all modules together. Every positive action—completing a task, hitting a nutrition target, finishing a study block, or logging a workout—rewards the user with points. Accumulating points advances the user through a structured hierarchy of ranks, transforming daily self-improvement into a game.

The system is optimized for a lightweight, serverless architecture using \*\*Next.js\*\* and \*\*Prisma ORM\*\*, ensuring zero-cost operation on free tier infrastructures (Vercel \+ Supabase/Neon) for a user base of 10–15 people.

\---

\#\# 2\. System Architecture & Tech Stack

\[ Client Browser: React \+ Tailwind CSS \] │ ▼ (Next.js Server Actions / API Routes) \[ Hosting Server: Vercel \] │ ▼ (Prisma Client) \[ Database: Supabase/Neon PostgreSQL \] 

\#\#\# 2.1 Backend & Deployment Infrastructure \* \*\*Hosting & Framework:\*\* Next.js hosted on \*\*Vercel\*\* (Free Tier). Next.js Server Actions or API routes will be used as backend handlers, removing the need for a separate express/node backend server. \* \*\*Database & Database Access:\*\* \*\*Prisma ORM\*\* mapping to a managed \*\*PostgreSQL\*\* instance provided by \*\*Supabase\*\* or \*\*Neon.tech\*\* (Free Tier). This relational engine ensures reliable transactional states across multi-module records. \* \*\*Authentication Engine:\*\* \*\*Clerk (Free Tier)\*\* or \*\*NextAuth.js\*\*. The application will enforce strict tenant isolation, ensuring users only access their personal telemetry records. \--- \#\# 3\. Epics & Functional Module Requirements \#\#\# 3.1 Core Authentication & Session Guard \* \*\*REQ-AUTH-01:\*\* The system must restrict all application access behind an authentication barrier. Unauthenticated users must be redirected to a dedicated landing/login page. \* \*\*REQ-AUTH-02:\*\* The system must support isolated user registration and secure credential processing. \* \*\*REQ-AUTH-03:\*\* All internal relational tables must contain a foreign key constraint referencing the authenticated user's unique identifier (\`userId\`), guaranteeing total data isolation across accounts. \#\#\# 3.2 System Core: Dashboard & Gamification Engine The dashboard acts as the central data aggregation tier and displays the gamified telemetry of the entire application. \* \*\*REQ-DASH-01 (Gamification Card):\*\* The page header must show the user's current profile, current \*\*Rank\*\*, total aggregated points, and an animated progress indicator mapping current points to the next threshold boundary. \* \*\*REQ-DASH-02 (Weekly Consistency Heatmap):\*\* The UI must render a continuous 7-day calendar matrix (Monday through Sunday). A day box must light up using variable Tailwind green color gradients when a user fulfills a designated combination of daily habits or module operations. \* \*\*REQ-DASH-03 (Summary Matrix Widgets):\*\* The main viewport must embed read-only telemetry widgets from sub-modules, specifically: \* Today's active muscle group routine. \* Today's running calorie/protein balances vs. targets. \* Remaining pending high-priority tasks. \* \*\*REQ-GAME-01 (Points Engine Automation):\*\* The system must compute and increment a user's point ledger upon completion of structural module events using the following point distribution matrix: | Module Event Code | Trigger Action Description | Point Reward Value | | :--- | :--- | :--- | | \`EVT-TASK-COMP\` | Toggling a task item to completed state | $+5 \\text{ pts}$ | | \`EVT-STUDY-DONE\` | Marking a Study Table block as completed | $+15 \\text{ pts}$ | | \`EVT-WORKOUT-LOG\` | Submitting a completed daily workout plan | $+20 \\text{ pts}$ | | \`EVT-NUTR-GOAL\` | Meeting daily calorie and water consumption quotas | $+10 \\text{ pts}$ | \* \*\*REQ-GAME-02 (Rank Escalation):\*\* The backend must evaluate total points against a hardcoded rank tier table on every point mutation event: \* \*\*Rank 0:\*\* Novice ($0 \- 499 \\text{ pts}$) \* \*\*Rank 1:\*\* Apprentice ($500 \- 1499 \\text{ pts}$) \* \*\*Rank 2:\*\* Specialist ($1500 \- 3499 \\text{ pts}$) \* \*\*Rank 3:\*\* Expert ($3500 \- 6999 \\text{ pts}$) \* \*\*Rank 4:\*\* Master ($7000+ \\text{ pts}$) \#\#\# 3.3 Module A: Learning Module A three-tier suite tracking educational objectives, schedule execution, and historical assessment tracking. \#\#\#\# 3.3.1 Study Table \* \*\*REQ-LEARN-01:\*\* Users must be able to manage an internal catalog of learning subjects. \* \*\*REQ-LEARN-02:\*\* Users must be able to log a study entry specifying the target subject, allocation of time duration, and a boolean execution checkbox. \* \*\*REQ-LEARN-03:\*\* The sub-module must provide a configurable configuration panel allowing users to model a repeatable study timetable calendar layout. \#\#\#\# 3.3.2 Revision Box \* \*\*REQ-LEARN-04:\*\* Users must be able to register specific items or entire subject modules requiring active revision cycles. \* \*\*REQ-LEARN-05:\*\* The interface must feature a scheduling modal enabling users to forecast, assign, and arrange a timeline for future revision dates. \#\#\#\# 3.3.3 Exam Card \* \*\*REQ-LEARN-06 (Analytics Visualizer):\*\* The view must render an analytical line chart plotting historical performance. The X-axis must list \`Exam Date\` and the Y-axis must plot \`Achieved Marks / Percentage\`. \* \*\*REQ-LEARN-07 (Peak Metric Callout):\*\* The top profile matrix of the view must contain a metrics summary displaying the user's highest historical exam percentage score. \#\#\# 3.4 Module B: Exercise Module A fitness routing engine designed to manage weekly target splits and track foundational body biometrics. \* \*\*REQ-EX-01 (Weekly Routine Configurator):\*\* The view must expose an interactive 7-column planner interface representing the days of the week. Each column must feature a selector enabling assignment to structural groups: \`Chest\`, \`Back\`, \`Arms\`, \`Abs\`, \`Legs\`, or \`Rest\`. \* \*\*REQ-EX-02 (Dynamic Today Dashboard Display):\*\* The core module view must read the current server system time, look up the target routine entry mapped to the active weekday, and present that specific target as "Today's Workout Focus". \* \*\*REQ-EX-03 (Biometrics Tracker):\*\* The system must provide data fields to record changing user \`Weight (kg)\` and \`Height (cm)\` logs. \#\#\# 3.5 Module C: Food Module A tracking log for dietary consumption, macronutrient targets, and water hydration. \* \*\*REQ-FOOD-01 (Food Catalog Builder):\*\* Users must be able to maintain a custom dictionary of food items, capturing custom macronutrient density signatures per 100g unit: \`Calories (kcal)\`, \`Protein (g)\`, \`Carbohydrates (g)\`, and \`Fats (g)\`. \* \*\*REQ-FOOD-02 (Daily Diary Logging System):\*\* Users must be able to log a food consumption entry by pulling a record from their food catalog and providing an operational weight/quantity multiplier. Total macronutrient updates must aggregate instantly across the active daily log view. \* \*\*REQ-FOOD-03 (Hydration Counter):\*\* The module must surface a rapid-access glass UI component. Clicking this component must execute a discrete transaction adding exactly $+250\\text{ ml}$ to the user's running daily water consumption tracker. \#\#\# 3.6 Module D: Money Module A clean, transaction-based personal cash flow monitor. \* \*\*REQ-MONEY-01 (Bifurcated Ledger Interface):\*\* The system must lay out a clear double-entry layout, dividing inputs between an \*\*Earnings Registry Form\*\* and an \*\*Expenses Registry Form\*\*. \* \*\*REQ-MONEY-02 (Transaction Form Attributes):\*\* Each individual transaction entry must strictly record: \`Amount (Decimal/Float)\`, \`Category Tag (Dropdown Selection)\`, and \`Transaction Date\`. \* \*\*REQ-MONEY-03 (Dynamic Cash Balance Matrix):\*\* The top section must render a calculated balance indicator based on the algebraic sum of user logs: $$\\text{Net Balance} \= \\sum \\text{Earnings} \- \\sum \\text{Expenses}$$ \#\#\# 3.7 Module E: Task Module A streamlined task execution manager. \* \*\*REQ-TASK-01:\*\* The module must support standard task life cycles (Create, Read, Update, Delete). \* \*\*REQ-TASK-02 (Time-Horizon Filter Sorting):\*\* The interface must provide interactive filters allowing tabs to dynamically sort items into contextual scopes: \`Today\`, \`Upcoming\`, and \`Completed\`. \* \*\*REQ-TASK-03:\*\* Marking a task item as completed must instantly trigger a webhook payload or client callback updating the user's global gamification balance via \`EVT-TASK-COMP\`. \--- \#\# 4\. Proposed Database Schema Architecture (Prisma Model Blueprint) \`\`\`prisma datasource db { provider \= "postgresql" url \= env("DATABASE\_URL") } generator client { provider \= "prisma-client-js" } model User { id String @id @default(uuid()) email String @unique username String? totalPoints Int @default(0) currentRank String @default("Novice") height Float? // In cm, for Exercise Module weight Float? // In kg, for Exercise Module createdAt DateTime @default(now()) tasks Task\[\] studyLogs StudyLog\[\] revisions Revision\[\] exams Exam\[\] workoutSplits WorkoutSplit\[\] foodLogs FoodLog\[\] transactions Transaction\[\] } model Task { id String @id @default(uuid()) userId String title String isCompleted Boolean @default(false) dueDate DateTime? createdAt DateTime @default(now()) user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model StudyLog { id String @id @default(uuid()) userId String subject String durationMin Int isCompleted Boolean @default(false) logDate DateTime @default(now()) user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model Revision { id String @id @default(uuid()) userId String subject String plannedDate DateTime isCompleted Boolean @default(false) user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model Exam { id String @id @default(uuid()) userId String subject String marksGained Float totalMarks Float percentage Float // Computed field: (marksGained / totalMarks) \* 100 examDate DateTime user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model WorkoutSplit { id String @id @default(uuid()) userId String dayOfWeek Int // 1 \= Monday, 7 \= Sunday focusGroup String // Chest, Back, Arms, Abs, Legs, Rest user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model FoodLog { id String @id @default(uuid()) userId String foodName String calories Float // aggregated scaling based on quantity protein Float quantityGrams Float waterIntakeMl Int @default(0) logDate DateTime @default(now()) user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } model Transaction { id String @id @default(uuid()) userId String type String // EARNING or EXPENSE amount Float category String date DateTime user User @relation(fields: \[userId\], references: \[id\], onDelete: Cascade) } 

## **5\. Non-Functional & Operational Requirements**

* **NFR-PERF-01 (Optimistic UI Rendering):** Actions that mutate user points (such as task toggles or water logging) must employ React optimistic state updates. The UI state must advance instantly while database operations resolve concurrently in the background.  
* **NFR-COST-01 (Zero-Cost Infrastructure Cap):** The codebase must remain completely stateless outside the PostgreSQL engine. Server logic must fall well within Vercel's hobby computation allowances, and storage indexes must operate safely under Supabase's 500MB free database limits.  
* **NFR-SEC-01 (Row-Level Multi-Tenant Isolation):** Every operational Prisma lookup query must append an absolute filtering argument matching the logged-in session context (`where: { userId: session.userId }`). Cross-tenant visibility is classified as a critical system vulnerability. """

file\_name \= "nect-product-requirements.md" with open(file\_name, "w") as f: f.write(markdown\_content)

print(f"Successfully generated {file\_name}")

